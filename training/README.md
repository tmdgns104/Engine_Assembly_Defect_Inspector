# Training Workspace

This area contains all training-specific resources for V0 and V1.

`training/configs` stores dataset and experiment configuration files.
`training/scripts` stores train/eval/export command wrappers.
`training/experiments` stores experiment records, metrics, and results.
`training/datasets` stores non-sensitive dataset metadata and small manifests only.
