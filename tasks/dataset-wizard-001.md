# DATASET-WIZARD-001 — 전체 수집 계획을 안내하는 촬영 마법사

Status: DONE / VERIFIED — 소프트웨어 구현/합성 입력 검증 범위
Baseline: 60e5fb1
요구 근거: 사용자가 제공한 `DATASET_WIZARD_001_Codex_prompt_KO.md` (원본 수정/자동 Git 편입 없음).

## 범위와 재사용

동일 Windows 촬영 패키지/실행 도구에 초보자용 기본 화면과 계획 관리 계층을 추가한다.
CameraClient / CollectionSession.save / 기존 capture_proxy 저장기와 verifier를 재사용한다.
제품 상태/지시는 기존 profile/display/guide를 읽고 계획 JSON과 분리한다.
기존 GuidedRound의 사진별 human accepted는 호출하지 않는다. 새 batch-review 정책 v1은 별도 기록이다.
기존 수동 앱과 v1/v2 읽기는 고급 수동 모드에서 유지한다. Runtime/DB/HMI 아키텍처 변경 없음.

## 수용 조건

- 짧은 카메라/조명/검사영역/L-R/정상·빈자리 기준 확인, setup 사본 및 원본 보존.
- 준비 시험(최대12)과 4회차 본수집(지원 조명에 따라240 또는80)의 자동 순서. 제품별 계획 사본/해시 고정.
- 회차 → 조명 → 위치 → 상태. 기준 위치에서 계산한 화면 전용 윤곽·방향/조명 그림.
- 준비 한 번 → 유한 대기/새 프레임 안정성 → 원본1장. pause/취소/중복/고장 차단.
- ROI 품질 보조와 경고 확인, 상태별 묶음 비교·확대·개별 재촬영. 묶음 확인 전 자동 human accepted 없음.
- OS writer lock, append-only 이벤트, 저장 전 intent와 기존 manifest 대조 재개. 원본/거절/부분 실패 보존.
- origin_group의 용도 충돌 차단, 새 회차가 자동 독립 자료가 되지 않도록 사람 확인. 시험용 예약/합성/과거사진 분리.
- 상태/조건별 수량·미수집/보류·이력, 원본 변경 없는 라벨링 인계 목록. 빈 YOLO label/가짜 box 없음.
- 원래111개 테스트 보존 + 전체 계획/실패/재개/GUI 추가 검증, 한국어 안내.
- Windows만 사용. Jetson/CAD/학습/라벨링/유료 API/전역 Skill·Hook 변경 없음. T05 TODO 유지.

## 구조와 검증 계획

`wizard_plan.py`: 설정 검사·계획 전개·대상 윤곽 기하.
`wizard_quality.py`: ROI 보조 지표·유한 준비/안정성 게이트·미리보기 전용 그림.
`wizard_store.py`: 단일 writer 잠금과 append-only 검증 이벤트.
`wizard.py`: 계획/setup/회차/시도/묶음 확인/복구/인계 상태.
`wizard_app.py`: 큰 영상과 현재 물리 행동 중심 Tk 화면, 접힌 고급 설정.
공통 저장/카메라 구현은 복제하지 않는다. 모든 합성 테스트는 임시/ignored 경로, 원래 실제4장은 해시 보호한다.

## 진행 체크포인트

- 요구/기존 코드 확인 완료. baseline master/origin 일치. 기존 CAD/사용자 원본은 별도로 보존.
- 계획/품질/영속 상태와 기존 실행 파일의 기본 Tk 화면 연결 완료. 기존 수동 모드는 유지.
- 준비 시험+240단계 모의 실행, 저장/검토 중단 복구, 개별 재촬영, 새 제품/계획 버전, origin 용도 충돌 검증 진행.
- 실제 창에서 합성 입력의 준비→저장→다음 상태와 묶음 비교를 확인. 비교 창 크기 대응을 보완.
- 최종 전체130/130 PASS (기존111 + 추가19), 보호149개 파일 SHA256 일치, 패키지42개 버전 동일.
- 실제 Tk 창에서 큰 미리보기·준비 후 한 장 저장/다음 상태·네 장 비교·확대·재촬영 선택란 표시 확인.
- 실제 새 데이터 촬영0장. 기존 실물4장 보존, 새 계획 완료 수량0. Jetson/CAD/학습/라벨링/T05 상태 변경 없음.

## 결과 / Evidence

`docs/verification/DATASET-WIZARD-001.json`: 최종 테스트 수, 원본/환경 보호, 현재 소스 SHA256, 검증 경계.
`training/capture_windows/DATASET_WIZARD_KR.md`: 실행·초기 설정·준비/묶음 확인·재촬영·재개·인계 설명.

요구한 소프트웨어 수용 조건은 PASS. Full plan 테스트는 240 본수집 +12 준비 시험 +2 기준 사진을 **합성 입력**으로 처리했다.
실제 USB/물리 배치/조명/품질 지표 적합성은 이번 실행에서 UNVERIFIED. 모델 정확도나 데이터 충분성을 검증하지 않았다.
서로 다른 상태 수의 가상 제품 검증은 엔진 실물 검사 성공이 아니다.
Tk 버튼의 Space 키 해제 기본 동작까지 차단해 긴 키 누름 후 두 번째 촬영이 발생하지 않게 검증했다.
닫은 Tk 대화창의 콜백/변수 정리를 UI 스레드에서 수행해 저장 worker의 Tk finalizer 오류를 예방했다.

다음 사람 행동: 기존 실행 파일 → 시작 / 이어하기 → 새 수집 계획 → 카메라/조명/기준 영역 확인.
V0-T05 및 후속 학습은 자동 시작하지 않는다.
